/target
*/target
Cargo.lock
*proptest-regressions*

.bin/
pkg/
dist/
wasm-pack.log
node_modules/

*.step
*.stp
*.vtp
*.vtu
*.obj
*.json
*.png
*.jpg
*.stl

/.vscode
/.DS_store

# Sketch crate is deferred upstream-sync work; agents stage it here but
# it does not ship in `monstertruck` releases yet. Keep it untracked so
# wildcard `git add monstertruck-*/Cargo.toml` invocations don't sweep
# its manifest in by accident.
/monstertruck-sketch